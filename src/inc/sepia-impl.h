#ifndef ASSIGNMENT_SEPIA_FILTER_SEPIA_IMPL_H
#define ASSIGNMENT_SEPIA_FILTER_SEPIA_IMPL_H

#include "image.h"
#include "utils.h"

struct image sepia_c_impl(struct image);

struct image sepia_asm_impl(struct image);

#endif
